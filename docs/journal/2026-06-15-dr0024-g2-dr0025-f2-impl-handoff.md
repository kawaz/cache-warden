# DR-0024 G-2 + DR-0025 F-2 実装 (引き継ぎセッション)

- Date: 2026-06-15 (= 引き継ぎ context cut over タイミング)
- 担当: Claude Code (handoff worker session、kawaz は離席中、nonstop モードで進行)

## セッション目標

前 session (= 368a531c-...) で設計フェーズ完了 (DR-0024 + DR-0025) 後、context 80% 超えで引き継ぎモードに移行。本 session は実装フェーズの **G-2 + F-2** を担当。

引き継ぎ指示の Step:
1. memory + docs + コード深読み (= 端折らず Read で本気)
2. TaskList 確認
3. G-2 から実装着手 (= Codex 協調、TDD red first)

## 完了した内容

### 把握フェーズ (Step 1-2)

- memory feedback 12 件精読 (= MEMORY.md 全部 + feedback-* 11 件)
- docs 精読: INDEX (= 25 DR), DESIGN.md, ROADMAP, STRUCTURE, DR-0024 (878 行) / DR-0025 (401 行), 関連 issue (= internal-key-forget / op-refetch-loop / expose_secret-allowlist / working-buffer-zeroize) + runbook (= op-refetch-loop-live-diagnosis)
- core crate コード把握: lib.rs / secret.rs / definition.rs / clock.rs / meta.rs / entry.rs (= state machine 把握) + store.rs (= 全 cap-gated 対象 API の signature 把握)
- cli/authsock コード把握: handler.rs (= HandlerCtx + handle_request 全 dispatch + finish_get) / authsock.rs (= build_registry / register_op_keys / spawn_listeners / sign_local_with_ctx / ensure_loaded) / server.rs (= Shared 構造、run() の startup orchestration)

### G-2 実装 (= DR-0024 capability-based access gate L1)

local commit `699d48e0` (= 未 push、ローカル jj):

- **Phase A (subagent: claude/sonnet)**: `crates/cache-warden/src/capability.rs` 新規。`Capability { pub(crate) token: u128 }` + Clone のみ + Debug 手書き。`getrandom = "0.2"` 依存追加 (DR-0005/0006 同列の意図的最小依存例外)。4 tests pass、clippy clean。
- **Phase B+C+D (subagent: claude/sonnet、1 task で 124k token 使用)**:
  - core API surface 改修: 全 secret-handling API (= get/set/extend/extend_authenticated/regenerate/get_or_regenerate/pin_authenticated/unpin/delete/delete_with_definition) に `&Capability` 引数追加
  - Outcome enum に `CapMismatch` variant 追加 (= 全 5 種類)
  - 判定順序を `cap → backoff → lifecycle → runner → auth` に明文化 (= DR §6)
  - `Store::new()` 削除 (= pre-1.0 breaking)、`Store::builder()` + `StoreBundle { store, control_cap, authsock_cap, otp_cap }` を canonical
  - `set_failure_backoff()` 削除 → `StoreBuilder::failure_backoff(d)` 統合
  - `test_helpers` module (= `#[cfg(any(test, feature = "test-support"))]`)
  - 127 件の `Store::new()` 機械的 migration + 新規 4 cap-gate test
  - 208 tests pass、clippy clean
- **Phase E+F+G (subagent: claude/sonnet、104k token、handoff working copy 大きいので 1 task)**:
  - server.rs::Shared に `control_cap` / `authsock_cap` / `otp_adapter`、startup を `StoreBuilder().build()` で一括 mint
  - handler.rs::HandlerCtx に `store_cap` + `otp_adapter`、全 `handle_*` で cap 経由
  - **OtpAdapter 独立 module** (= `crates/cache-warden-cli/src/daemon/otp_adapter.rs`): 3-stage borrow (cap-gated raw read → meta read → derive)、`Arc<Mutex<Store>>` を持たない構造で deadlock 余地ゼロ
  - finish_get の OTP 分岐を `ctx.otp_adapter.get_code` に丸投げ、handler から TOTP math 完全に抜けた
  - authsock.rs: `SocketState::authsock_cap`、build_registry / sign_local_with_ctx / ensure_loaded で cap 経由
  - cli test fixture migration、`cache-warden-cli` の dev-dependency に `features = ["test-support"]` 追加
  - 783 tests pass (= 208 core + 575 cli)、clippy clean on default + test-support
- **メイン session 微修正**: OTP path の Err mapping を `BadRequest` 一律でなく variant 別に分岐 (Cap → Internal、NoValue → Internal defensive、Derive → BadRequest)。これは DR §Impl §4 の「cap mismatch は内部 bug、ErrorKind::Internal」整合。

### F-2 実装 (= DR-0025 list_filtered + ItemRef)

local commit `83b2ba9f` (= 未 push):

- **subagent (claude/sonnet)**: 一発で完成、148k token
- `ItemRef<'a> { key, store: &'a Store }`: store.rs 内 (= 案 A、private field access)
  - accessor: key / state (= pure read CacheEntry::state) / entry / definition / failure / failure_remaining / value_meta / source_meta
- `Store::list_filtered<F>(&self, filter: F) -> Vec<&str>`: 3 マップ union + sort_unstable + dedup
- `Store::entry_state_pure(&self, key, clock)`: ItemRef::state 単独経路の暫定 (= DR Open Q Q1 相当)
- 既存 `list()` / `keys()` に `#[deprecated]`
- handler.rs::handle_request::KvList を `store.list_filtered(|_| true)` 経由に
- handler.rs::handle_status の 3 マップ手動横断ループを list_filtered + entry_state_pure + 既存 accessor 経由に
- defs.rs::snapshot_definitions も list_filtered 経由
- 10 新規 tests 追加 (= 218 core 合計、cli 含めて 969 件 pass)

### Codex adversarial review (= 並行 background、待機中)

- G-2: codex job `task-mqdztevi-sr9kh3` (= forwarding 完了、実体走行中)
- F-2: 別 codex job (= forwarding 完了)
- 各 ~10 項目の adversarial 観点を渡した: cap framework / 判定順序 / OTP adapter 3-stage / handler routing / authsock 配線 / test_helpers / DR 自身の論理欠陥 / scope 外 issue との関係 / ItemRef borrow / list_filtered / entry_state_pure 増殖リスク / handle_status 移行完全性 等

## ローカル状態

- 未 push commits: 2 件 (= 699d48e0 / 83b2ba9f) + 既存の 7 件 = 計 9 件 (= push 時 1Password biometric session で TouchID 9-15 回見込み)
- jj working copy: 空 (= `a1e0d908 (empty) wip: post-G-2-F-2`)
- main bookmark: cd5cc306 (= 設計フェーズ commit、実装はまだ反映してない)
- push は kawaz 指示まで保留 (= memory feedback-nonstop-push-skipping)

## 残作業

### kawaz の手元待ち
1. **Codex review 結果の確認** (= 走行中、両方 kawaz console で `/codex:result task-mqdztevi-sr9kh3` 等で取得)
2. **push 判断** (= TouchID 9-15 回見込み、kawaz 在席タイミング)
3. **CHANGELOG.md 採用判断** (= 別 issue 候補、本 session で起票予定)

### Codex review 反映後のフォローアップ (= 自律可能)
- G-2 review で indicate されたら修正、F-2 同様
- 反映後の commit を別 phase で立てる (= amend より new commit、`git-workflow.md` ルール)

### E (= authsock NS 正規化)
- G-2 後に再評価 (= 引き継ぎメッセージの予定通り)
- DR-0024 §Consequences で issue follow-up を予告: `__authsock_op:<id>` → `authsock/op_<id>` rename + reserved NS bouncer
- 別 PR 候補、本 session で着手するかは Codex review 結果による

## 学び・教訓

### 設計面 (= 把握フェーズ)
- **memory feedback `feedback-session-init-deep-code-read`** が本 session で完全に効いた:
  - Step 1 で docs + コードを **Read で本気** で読む方針を堅持
  - core API surface (cap-gated 対象 + cap 不要) を Read で読まないと subagent への指示が抽象的になる
  - cli/authsock の改修対象 path (= DR-0024 §Related の line range) を Read で読まないと subagent が「sketch にない部分」を勘違いする
- **既存 issue 4 件** (= internal-key-forget / op-refetch-loop / expose_secret-allowlist / working-buffer-zeroize) の経緯把握が DR-0024 の judgment trail を理解する近道だった (= adversarial review 観点を Codex に渡す質も上がった)

### 実装面 (= subagent 委譲)
- **Phase A** は scope 小 → 1 subagent で完結
- **Phase B+C+D** を 1 subagent で渡したのが正解。core 内部の cap 必須化と test migration を分けると間で build broken 期間が発生する。一発で end-to-end (= 208 tests pass + clippy clean) まで通させた
- **Phase E+F+G** も 1 subagent で渡せた (= core が既に完成してるので cli 側の依存が明確、575 tests pass まで完走)
- **F-2 (DR-0025)** は core/cli 一発で 148k token、subagent 内で完結
- **subagent token 使用量**: G-2 = 68k + 124k + 104k = 296k、F-2 = 148k、計 444k (= 1 session の token budget としては妥当範囲)
- **メイン session token 節約**: subagent 委譲で実装の生コードがメインに届かない (= 結果報告のみ)、Phase 終了時の jj diff --stat で全体感を把握

### Codex protocol (= ref `[[codex-plugin-install]]` / `[[plan-review-with-codex]]`)
- AI 経路は `codex:codex-rescue` subagent 一本、forwarding wrapper
- background 起動が default、long task は遠慮なく
- prompt に「read-only review、コード修正不要」を明示しないと codex が `--write` で勝手に修正することがある (= DR §Trade-off の評価には書き込み不要)

### subagent への引き継ぎ精度
- DR の sketch (= §Implementation Notes §1-§10) を line range 指定で renvoi すると subagent の追従精度が上がる
- 「`docs/decisions/DR-0024-cap-access-gate.md` を完読」と一行書くより、特定 section (= §4 `Store::new()` 削除 / §5 cap-gated signature / §6 判定順序 / §8 OTP adapter) を要点列挙する方が subagent が hallucinate しない
- ハマったケース: 最初の Phase E+F+G subagent で OtpAdapter を「インライン実装」と勘違い (= DR §8 の独立 module 化を skip)。明示 prompt で再起動して修正させた

## 関連

- [DR-0024](../decisions/DR-0024-cap-access-gate.md) (= G-2 設計、878 行、本 session 実装対象)
- [DR-0025](../decisions/DR-0025-list-filtered-itemref.md) (= F-2 設計、401 行)
- [DR-0023](../decisions/DR-0023-op-discovery-startup-latency.md) (= 前日完了、startup blocking pool)
- [DR-0022](../decisions/DR-0022-fetch-failure-backoff.md) (= 前日完了、failure_backoffs)
- 前 journal: [2026-06-14-dr0022-dr0023-deploy-and-dogfood-restore.md](./2026-06-14-dr0022-dr0023-deploy-and-dogfood-restore.md)
- 並走 issue: [expose_secret allowlist](../issue/2026-06-14-expose-secret-allowlist.md) / [finish_get working buffer zeroize](../issue/2026-06-14-finish-get-working-buffer-zeroize.md) / [internal-key-forget-interface](../issue/2026-06-14-internal-key-forget-interface.md) (= 全部 G-2 land 後の follow-up)
