# v0.23.0 リリースウェーブ (issue 一掃 + セキュリティ強化 + startup 非ブロック化)

## 含まれる変更 (main push 済み、af6f7500 = Release v0.23.0)

- **DR-0026**: op discovery 失敗時の disk-cache fallback (0-key serving 解消、
  provenance 境界)
- **DR-0027**: コア公開 API での合成キー文法強制 + `OpKvKey` + 予約 NS 書込 bouncer
  (breaking minor)
- **DR-0028**: `SecretBytes::with_exposed` scope 限定アクセサ、`expose_secret`
  非公開化 (breaking minor)
- **DR-0023 Phase 2**: listener 先行 bind + disk-cache seed + source 単位 background
  discovery (issue 2026-06-13 の P1 解消。Workflow の多視点レビューで major 4 バグ
  — all_fresh all-or-nothing / sign 中の registry read guard 長期保持 / multi-source
  cache clobber / async worker 上の blocking lock — を land 前に検出・修正)
- **release.yml semver gate** (canonical 化 + Cargo.toml trigger 向け green-skip
  順序)。v0.23.0 の実 CI で check-version job の正常動作を確認済み
- push 後 (v0.23.0 対象外、次リリースに乗る): 予約 NS の **kv.get 拒否**
  (read bouncer、`91735da6`)、justfile の notify+watch canonical 同期

## リリースのハマり所: notarize 403 (PLA 再同意)

Release workflow が Notarize step で `HTTP 403. A required agreement is missing or
has expired` により失敗 (rerun でも同一 = transient でない)。Apple Developer の
License Agreement 再同意が必要で、**kawaz の Apple ID 操作でしか解消できない**。
macos-signing skill の「notarize 403 PLA 再同意」既知パターン。

**解決 (同日)**: kawaz が PLA 再同意 → 同意直後の rerun はまだ 403 (runbook の
「1 分以内に反映」より遅い) → **5 分待って rerun したら成功**。skill runbook の
伝播時間の記述は楽観的すぎる場合がある (数分待ってからの rerun を推奨)。

## Phase 2 の実機検証 (v0.23.0 daemon 再起動、同日)

- `startup complete in 0.02s`、register 完了から **0.70s で socket 列挙可能**
  (旧: launchd で op timeout の ~30s 待ち)
- bind 時は 0 op 鍵 (旧 cache は provenance 無し = seed 除外、DR-0026 の保守規則
  どおり) → **background discovery が 6 op 鍵を発見し稼働中の 3 registry を
  hot-update** (`op discovery updated registry (6 op key(s))`)。新形式 cache が
  保存されたので次回再起動からは op 鍵も即 seed される
- 今回は biometric session が温かく op が即応答 (P3 は発現せず、P3 自体は残存)
- 一回性の異常: 最初の `daemon register` (旧 v0.22.3 稼働中に実行) が help を
  出力して bootout 状態で中断、再実行で正常復旧。
  issue `2026-07-06-daemon-register-help-output-oneshot` で追跡

## 残 issue の状態 (2026-07-06 時点)

- **kawaz の設計判断待ち**: `crate-macos-process-inspect` (+ blocked 2 件:
  `custom-touchid-dialog` / `kv-get-peer-identity-guard`)、
  `ssh-agent-provider-architecture` (idea)、`touchid-blocks-blocking-pool`
  (再評価条件成立、案 X/Y/Z の選択が Provider 再設計と不可分 → issue に追記済み)、
  `graceful-restart-state-handoff` (idea)
- **pre-1.0 まで保留**: `changelog-md-adoption` (pending-sublimation)
- **実機検証待ち**: Phase 2 の launchd 実機での bind 即応確認、launchd biometric
  制約 (P3) の対処方針
